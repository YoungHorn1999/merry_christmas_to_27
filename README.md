# Merry Christmas Tree 🎄

这是一个基于 Three.js 的 3D 圣诞树特效项目。

## ✨ 功能
- **3D 旋转圣诞树**：由粒子组成的精美圣诞树。
- **交互效果**：
  - **点击屏幕**：圣诞树会“爆炸”成粒子特效，再次点击复原。
  - **移动鼠标**：可以轻微旋转视角。

## 🚀 如何启动

### 1. 本地直接运行（最简单）
- 在文件夹中找到 `index.html` 文件。
- 双击该文件，它将会在你的默认浏览器中打开。

### 2. 使用 macOS 终端
在当前目录下运行：
```bash
open index.html
```

### 3. 本地服务器（推荐）
为了避免浏览器的安全限制，推荐使用 Python 启动一个简单的 HTTP 服务器：

```bash
python3 -m http.server 8000
```
然后在浏览器访问：[http://localhost:8000](http://localhost:8000)

## 🌐 在线部署 (GitHub Pages)

由于你已经将代码推送到 GitHub，可以按照以下步骤开启在线访问：

1. 访问你的仓库：[https://github.com/YoungHorn1999/merry_christams_tree](https://github.com/YoungHorn1999/merry_christams_tree)
2. 点击 **Settings** (设置) -> 左侧 **Pages**。
3. 在 **Branch** 处选择 `main`，点击 **Save**。
4. 稍等片刻，你就可以通过类似 `https://YoungHorn1999.github.io/merry_christams_tree/` 的链接访问了。

